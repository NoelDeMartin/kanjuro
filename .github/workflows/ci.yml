name: CI
on: [push]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Lint
        run: shfmt -d -l .
      - name: Run Shellcheck
        run: shellcheck kanjuro ./scripts/*
