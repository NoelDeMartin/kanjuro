{
    "emeraldwalk.runonsave": {
        "commands": [
            {
                "match": ".*",
                "cmd": "shfmt -l -w ${file} && shellcheck -f diff ${file} | git apply"
            }
        ]
    }
}
